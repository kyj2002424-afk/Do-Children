/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/Do-Children',
  assetPrefix: '/Do-Children/',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;

